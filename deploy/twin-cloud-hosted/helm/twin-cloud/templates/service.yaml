apiVersion: v1
kind: Service
metadata:
  name: {{ include "twin-cloud.fullname" . }}
  labels:
    {{- include "twin-cloud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "twin-cloud.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
