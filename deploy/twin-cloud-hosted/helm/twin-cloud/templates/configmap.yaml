apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "twin-cloud.fullname" . }}-config
  labels:
    {{- include "twin-cloud.labels" . | nindent 4 }}
data:
  SPANDA_TENANT_ID: {{ .Values.tenantId | quote }}
