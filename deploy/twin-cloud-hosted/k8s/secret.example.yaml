apiVersion: v1
kind: Secret
metadata:
  name: twin-cloud-api-key
  namespace: spanda-twin-cloud
type: Opaque
stringData:
  # Replace before apply — or use External Secrets / Sealed Secrets
  SPANDA_API_KEY: change-me-operator-key
