# spanda-camera

Camera sensor adapters (stub) for vision and motion scoring pipelines.

**Status:** Experimental stub — import-only scaffold.

**Live backend (experimental):** `SPANDA_LIVE_CAMERA=1` with `SPANDA_CAMERA_CMD='echo 1280,720,0.15'` (`width,height,motion_score`).
