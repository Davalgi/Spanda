[package]
name = "spanda-camera"
version = "0.1.0"
description = "Camera sensor adapters (stub)"
license = "Apache-2.0"

[safety]
level = "sensors"

[capabilities]
required = [ "sensors.camera.read" ]

[hardware]
requirements = []
