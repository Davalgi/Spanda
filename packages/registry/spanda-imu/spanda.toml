[package]
name = "spanda-imu"
version = "0.1.0"
description = "IMU sensor adapters (stub)"
license = "Apache-2.0"

[safety]
level = "sensors"

[capabilities]
required = [ "sensors.imu.read" ]

[hardware]
requirements = []
