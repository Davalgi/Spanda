# spanda-imu

IMU sensor adapters (stub) for localization and fusion pipelines.

**Status:** Experimental stub — import-only scaffold.

**Live backend (experimental):** `SPANDA_LIVE_IMU=1` with `SPANDA_IMU_CMD='echo 0.02,-0.01,1.57,0,0,9.81'` (`roll,pitch,yaw,ax,ay,az`).
