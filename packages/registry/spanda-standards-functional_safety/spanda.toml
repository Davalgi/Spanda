[package]
name = "spanda-standards-functional_safety"
version = "0.1.0"
description = "Standards profile scaffold for functional_safety — required checks/evidence via packages, not embedded regulatory text"
license = "Apache-2.0"

[safety]
level = "robotics"

[capabilities]
required = ["governance.standards.functional_safety"]
