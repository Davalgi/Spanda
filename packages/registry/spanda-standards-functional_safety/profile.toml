# Standards profile reference — implement checks via plugins/packages.
# Spanda does not embed regulatory text.

kind = "functional_safety"
required_checks = []
required_evidence = []
required_reports = []
required_approvals = []
required_documentation = []
