[package]
name = "spanda-standards-industrial_safety"
version = "0.1.0"
description = "Standards profile scaffold for industrial_safety — required checks/evidence via packages, not embedded regulatory text"
license = "Apache-2.0"

[safety]
level = "robotics"

[capabilities]
required = ["governance.standards.industrial_safety"]
