[package]
name = "differentiation-warehouse"
version = "0.1.0"
description = "Differentiation NOW showcase — mission contracts, explain, decision audit"
license = "Apache-2.0"

[hardware]
targets = ["RoverV1"]

[capabilities]
required = ["lidar.read", "motion.propose", "gps.read"]

[safety]
level = "experimental"
