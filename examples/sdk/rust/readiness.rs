//! Evaluate readiness via the official Rust SDK.
//! Requires Control Center: `spanda control-center serve --config examples/robotics`
//!
//! Run: `cargo run --example readiness -p spanda-sdk`

fn main() {
    // See crates/spanda-sdk/examples/readiness.rs
    eprintln!("Run: cargo run --example readiness -p spanda-sdk");
}
