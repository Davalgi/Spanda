//! List device entities via the official Rust SDK.
//!
//! Run: `cargo run --example device_inventory -p spanda-sdk`

fn main() {
    // See crates/spanda-sdk/examples/device_inventory.rs
    eprintln!("Run: cargo run --example device_inventory -p spanda-sdk");
}
