{
  "name": "robolang",
  "version": "0.1.0",
  "description": "A safe, strongly typed language for robot control and simulation",
  "type": "module",
  "bin": {
    "robolang": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "robolang": "node --import tsx src/cli/index.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
